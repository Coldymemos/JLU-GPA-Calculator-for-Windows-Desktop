# 桌面版施工细节文档 · JLU GPA Calculator for Windows Desktop

> 版本：v1.0（施工稿）　|　新仓库：https://github.com/Coldymemos/JLU-GPA-Calculator-for-Windows-Desktop
> 代码基线：从 Web 版（`WebEdition`，v1.0.1）复制核心代码，桌面版独立演进，**不再与 Web 版共享代码**。
> 前置规划：《本地化功能规划.md》（随代码一并复制进本仓库，作为功能范围依据）。

---

## 当前施工状态（2026-08-23）

- **M1 已完成**：Web/Tauri 双构建入口已分离，Windows 打包与 CI 已落地；
- **M2 已完成**：桌面运行时使用 SQLite Schema v2，Web 构建继续使用 IndexedDB；前端通过 `PersistencePort` 无感切换；
- 已支持多档案创建/切换/重命名/删除；课程、规则和设置按档案隔离，活动档案记录在 `meta`；
- 已支持 Web/桌面当前档案 JSON 双向迁移、全库 SQLite 备份、SHA-256 sidecar、恢复前 dry-run 和恢复前自动副本；
- 73 项前端单元/组件测试与 5 项 Rust 存储测试通过，`pnpm check` 全绿；
- Windows x64 发布构建已通过：应用约 11.13 MiB、MSI 约 4.45 MiB、NSIS 约 3.30 MiB；
- CI 已切换为 Windows Tauri 构建，`v*` 标签创建草稿 Release；
- **M3 已实现（2026-08-23 追加）**：目录选择与路径记忆、递归扫描、内容嗅探（不依赖文件名）、导入队列与逐文件报告、内容级 SHA-256 去重、低置信度人工确认、多工作表选择重试；
- **M4 已实现（2026-08-23 追加）**：清洗管道（去括号批注/单位后缀/空白合并，Web/桌面共用）、表头同义词扩展与包含式自动映射、列映射确认界面（手动指定列）、多规则集并行对照（另存新规则集 + 并行计算表格）；
- 下一阶段为 **M5 文件系统进阶**：目录监听（notify）、定时备份、导出直写路径、冲突处理。

---

## M4 实现说明（2026-08-23）

- **清洗管道** `src/domain/course/cleaning-pipeline.ts`：JSON 声明式规则（`pattern` + `flags` + `replacement`），内置三条默认规则（去括号批注 `90(重修)→90`、去单位后缀 `90.0分→90`、空白合并），NFKC 全角转半角显式执行；单条规则损坏不影响整体。导入器在成绩/学分/绩点字段统一走 `cleanRawValue`。
- **表头智能映射**：`fieldAliases` 同义词扩充（课程代码/课程编号/最终成绩/期末成绩/分数/绩点学分等）；自动映射采用**包含式匹配**（`课程号（必填）` 可识别），刻意不用编辑距离自动映射（中文短表头距离 1 误报严重，如 `课程名`/`课程号`）；异体字等低置信度表头进入**列映射确认界面**（`ImportDialog` 内为四个必要字段提供列下拉，选择后立即重解析）；`ImportPreview` 新增 `availableColumns`，解析器支持 `headerOverride` 手动映射覆盖。
- **多规则集并行模拟**：`PersistencePort` 新增 `listRuleSets`（Rust 新增 `rule_sets_list` 命令 + 单测，IndexedDB 侧直接 toArray）；规则设置抽屉新增名称输入与「另存为新规则集」；侧边栏新增「规则对照」面板（`ComparisonDrawer`），勾选多套规则集并行计算三项结果，与当前规则不同的单元格标记「差异」。
- 与 Web 基线兼容：`fieldLabels`/`requiredFields` 放在纯类型模块 `import.types.ts`，保证 `ImportDialog` 对解析器的懒加载不失效（构建无 `INEFFECTIVE_DYNAMIC_IMPORT` 警告）。

## M3 实现说明（2026-08-23）

与原施工稿的差异（受离线环境限制，无法新增 Rust crate）：

| 原设计                                               | 实际实现                                                                                                                                           | 原因                                                                                                            |
| ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| Rust `sniff_file`（calamine 读 xlsx/xls + 编码探测） | 前端 `src/infrastructure/importers/sniffer.ts`：魔数判格式 + SheetJS 读表头 + BOM/严格 UTF-8 失败回退 gb18030 编码探测 + 同义表头匹配              | calamine / encoding_rs / chardetng 无法离线拉取；前端复用既有 SheetJS 与 `fieldAliases`，口径与真实解析完全一致 |
| Rust `file_sha256` 由前端队列调用                    | Rust `file_sha256` 已实现并单测；前端队列对已读取字节用 Web Crypto 计算（同算法同结果），避免每文件二次 IO                                         | 队列需同时嗅探与解析，字节已在内存中                                                                            |
| capabilities 增加受控 fs 作用域                      | 不加 `tauri-plugin-fs`：文件 IO 全部走自研命令（`scan_directory` / `file_sha256` / `read_file_bytes`），路径仅来自用户在系统选择器中显式选择的目录 | 自研命令天然满足最小授权，且不引入新插件                                                                        |

M3 新增/改动文件：

- `src-tauri/src/importer.rs`（新）：目录扫描（扩展名候选 + 忽略 `~$` 临时文件）、SHA-256、字节读取（10 MB 上限），含 4 项 Rust 单测；
- `src-tauri/src/commands.rs` / `lib.rs`：注册 `scan_directory` / `file_sha256` / `read_file_bytes`；
- `src/infrastructure/importers/sniffer.ts`（新）：内容嗅探（`importable` / `needs-confirmation` / `not-a-grade-sheet`）；
- `src/infrastructure/importers/sheetjs-importer.ts`：导出 `fieldAliases` / `requiredFields` 供嗅探复用；
- `src/infrastructure/desktop/directory-importer.ts`（新）：目录选择、扫描、读字节、嗅探的 tauri 封装；
- `src/application/directory-import.ts`（新）：导入队列编排（串行、指纹去重、失败隔离、多工作表报告）；
- `src/ui/components/DirectoryImportDrawer.tsx`（新）：目录批量导入 UI（记忆上次路径、识别表、确认弹窗、逐文件报告）；
- `src/ui/App.tsx` / `src/ui/components/CourseDrawer.tsx`：桌面端「批量导入目录」入口。

---

## 1. 目标与硬性约束

| 约束     | 要求                                                            |
| -------- | --------------------------------------------------------------- |
| 发布体积 | **安装包 ≤ 1GB**（预算目标见 §10，Tauri 方案实际仅 5–20MB）     |
| 形态     | Windows 桌面应用（WebUI，Tauri 2 壳 + 现有 React UI）           |
| 数据     | 本地 SQLite 主存储；支持从 Web 版 IndexedDB 一键迁移；备份/恢复 |
| 网络     | 完全离线可用；运行时零联网依赖（OCR 模型按需下载除外，见 §9）   |
| 基线     | 计算引擎与 Web 版保持同一套算法口径（复制时点冻结为 v1.0.1）    |

## 2. 技术选型（定案）

| 层       | 选型                                                      | 版本/说明                          |
| -------- | --------------------------------------------------------- | ---------------------------------- |
| 壳       | Tauri 2                                                   | 复用系统 WebView2（Win10/11 预装） |
| 前端     | React 19 + TypeScript + Vite 8 + Ant Design 6             | 直接复用 Web 版 `src/`，首期零改动 |
| 桌面后端 | Rust（rusqlite + notify + tauri-plugin-dialog 等）        | 文件系统、存储、系统集成           |
| 存储     | SQLite（rusqlite，同步 API 适合本机单用户）               | Schema v2，见 §6                   |
| 解析     | 前端复用 SheetJS；Rust 侧备选 `calamine`（xlsx 原生解析） | 首期沿用前端解析链路               |
| 打包     | `tauri build` → NSIS / MSI                                | 体积预算见 §10                     |

## 3. 代码来源与仓库初始化

- **来源**：`D:\Personal\work\GPAcalculate\WebEdition`（当前工作树 = 分支 `Coldymemos-adaption`，含移动端适配与 v1.0.1 版本号）
- **复制清单**（进 `DesktopEdition/`）：
  - 复制：`src/`、`tests/`、`docs/`、`public/`、`.github/`、根配置（`package.json`、`pnpm-lock.yaml`、`pnpm-workspace.yaml`、`vite.config.ts`、`vitest.config.ts`、`tsconfig*`、`eslint.config.js`、`.prettierrc.json`、`.prettierignore`、`.gitignore`、`index.html`、`playwright.config.ts`）、`README.md`、`LICENSE`、`本地化功能规划.md`
  - **排除**：`.git/`（重新 init）、`node_modules/`（重新 install）、`dist/`、`test-results/`、`tsconfig.node.tsbuildinfo`
- **新仓库**：`git init -b main` → 首次提交 → `git remote add origin https://github.com/Coldymemos/JLU-GPA-Calculator-for-Windows-Desktop.git`
- **历史**：不携带 Web 版 git 历史（全新起点）；如需追溯对照，指向 Web 仓库即可

## 4. 目录结构（目标态）

```text
DesktopEdition/
├── src/                        # 前端（复用 Web 版，本地化功能在此增改）
│   ├── domain/                 #   计算引擎（冻结基线，改动需单测兜底）
│   ├── application/            #   用例层
│   ├── infrastructure/         #   导入/导出/持久化（存储实现将替换为 SQLite）
│   └── ui/                     #   React 界面
├── src-tauri/                  # Tauri 后端（新增）
│   ├── src/
│   │   ├── main.rs             #   入口
│   │   ├── lib.rs              #   tauri::Builder 装配
│   │   ├── commands/           #   文件系统 / 存储 / 备份 tauri 命令
│   │   ├── storage/            #   rusqlite 连接池、schema、迁移
│   │   └── importer/           #   内容嗅探、PDF 提取、清洗（Rust 侧）
│   ├── capabilities/           #   权限清单（最小授权）
│   └── tauri.conf.json
├── docs/                       # 规划与施工文档
├── tests/                      # 前端单测/e2e（复用 Web 版）
└── package.json                # 前端依赖（新增 @tauri-apps/*）
```

## 5. 施工阶段（M1→M6，每阶段含验收）

### M1 · Tauri 壳（地基）

步骤：

1. `DesktopEdition` 内 `pnpm install --frozen-lockfile` 恢复前端依赖
2. `pnpm add -D @tauri-apps/cli@^2`；`pnpm add @tauri-apps/api@^2`
3. `pnpm tauri init`（或手写 `src-tauri/`）：应用名 `JLU GPA Desktop`、identifier `com.coldymemos.jlugpa.desktop`、Windows 侧 WebView2
4. `tauri.conf.json`：`frontendDist: ../dist`、`beforeBuildCommand: pnpm build:desktop`、窗口默认 1280×800
5. `capabilities/default.json`：核心权限 `core:default`；文件权限**首期不加**（M3 再加目录授权）
6. 验证：`pnpm tauri dev` 启动、现有 Web 功能（导入/计算/导出）在桌面壳内可用；`pnpm tauri build` 出 NSIS 安装包

验收：桌面壳运行 Web 版全功能无回归；打包体积 < 30MB。

### M2 · 存储迁移（所有本地化能力的地基，最先定稿）

1. 定义 `PersistencePort`（前端接口）：`loadCourses / replaceCourses / appendCourses / saveCourse / removeCourse / clearCourses / loadRuleSet / saveRuleSet / loadSetting / saveSetting / hasAnyData / clearAllData / exportData / importData` —— IndexedDB 与 SQLite 使用同一契约
   桌面端另以 `ArchivePort` 提供 `listArchives / getActiveArchiveId / createArchive / renameArchive / deleteArchive / setActiveArchive`；Web 端保持单档案。
2. Web 版实现保持不变（IndexedDB）；桌面版新增 `SqlitePersistence`（经 tauri invoke 调 Rust）
3. Rust 侧 `storage/mod.rs`：打开/建库、`schema_version` 管理、迁移脚本链
4. **迁移向导 UI**（设置页新增入口）：
   - Web → 桌面：浏览器 IndexedDB 导出 JSON（新增 Web 版导出按钮）→ 桌面导入向导 → 校验 → 写入 SQLite
   - 桌面 → Web：SQLite 导出 JSON → Web 版导入（复用现有过滤配置导入的交互模式，扩展为全量）
5. 备份/恢复：`备份到…` 生成 `.db` 文件 + manifest（schema_version + sha256）；恢复前 dry-run 校验

验收：双端数据互通（Web 导出 → 桌面导入 → 计算结果一致）；备份损坏文件被拒绝且原数据保留。

### M3 · 批量导入（文件系统访问）

1. 目录授权：capabilities 增加 `dialog:allow-open` + 受控 `fs` 作用域（仅用户选择的目录）
2. 前端：目录选择器（记住上次路径，存 SQLite settings）→ 扫描（Rust `commands::scan_directory`，递归深度可配，按扩展名出候选清单）
3. **内容嗅探**：Rust `sniff_file`（xlsx: 读 sheet 头行 + 表头关键词匹配；csv: 编码探测 + 表头匹配；低置信度 → 前端确认界面，绝不静默导入）——**不依赖文件名**
4. 导入队列：前端串行调现有 `parseSpreadsheetFile`（或 Rust calamine 实现），逐文件产出报告（成功/跳过/失败+原因/可重试）
5. 去重：文件 sha256 指纹（Rust 计算）；课程级沿用「同课程号保留最高分」
6. 保留手动文件选择器入口

验收：把一个混合格式、乱命名文件的目录拖进来 → 正确识别导入、坏文件不影响其他、重复文件跳过。

### M4 · 解析增强

1. 表头同义词库：`src/infrastructure/importers/header-synonyms.json`（课程号/课程代码/编号…），匹配失败进映射确认界面
2. 清洗管道：正则规则（JSON 声明式），与 `course.normalizer.ts` 对齐，Web/桌面同一套
3. 多规则集并行模拟：规则集存 SQLite，计算页支持勾选多套并行计算、结果对照

验收：`成绩 90(重修)`、全角、多余空格等脏数据清洗正确；两套规则集结果并列展示。

### M5 · 文件系统进阶

1. `notify` crate 目录监听（防抖 300–500ms），新文件自动进队（设置可关）
2. 定时备份（每日/每周，保留 N 份）
3. 导出直写路径（记忆上次目录）
4. 冲突处理（跳过/覆盖/追加）在导入报告界面统一确认

### M6 · 解析进阶

1. PDF 成绩单：前端 `pdfjs-dist` 提取 → 坐标聚类表头行 → 复用清洗；失败兜底引导提供 xlsx/csv
2. 反推/敏感度：复用 `CourseEvaluation` 增量计算
3. OCR（按需下载模型到本地缓存目录，不进安装包）——体积红线保证

## 6. 数据模型 v2（SQLite DDL 草案）

```sql
PRAGMA foreign_keys = ON;

CREATE TABLE meta (
  key TEXT PRIMARY KEY,          -- schema_version / app_version / created_at
  value TEXT NOT NULL
);

CREATE TABLE archives (          -- 多档案（多年/多学生/备份副本）
  id TEXT PRIMARY KEY,           -- uuid
  name TEXT NOT NULL,
  created_at TEXT NOT NULL
);

CREATE TABLE courses (
  archive_id TEXT NOT NULL REFERENCES archives(id) ON DELETE CASCADE,
  id TEXT NOT NULL,
  code TEXT NOT NULL,
  name TEXT NOT NULL,
  term_year TEXT,
  term_semester TEXT,            -- spring/summer/autumn/winter/unknown
  public_elective_category TEXT,
  payload_json TEXT NOT NULL,    -- 完整 Course JSON 是无损存储的事实来源
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  PRIMARY KEY (archive_id, id)
);
CREATE INDEX idx_courses_archive_code ON courses(archive_id, code);
CREATE INDEX idx_courses_archive_term ON courses(archive_id, term_year, term_semester);

CREATE TABLE rule_sets (
  archive_id TEXT NOT NULL REFERENCES archives(id) ON DELETE CASCADE,
  id TEXT NOT NULL,
  payload_json TEXT NOT NULL,    -- 完整 AppRuleSet（含 gradePoint/recommendation/exclusions）
  PRIMARY KEY (archive_id, id)
);

CREATE TABLE settings (
  archive_id TEXT NOT NULL REFERENCES archives(id) ON DELETE CASCADE,
  key TEXT NOT NULL,
  value_json TEXT NOT NULL,
  PRIMARY KEY (archive_id, key)
);

CREATE TABLE course_history (    -- 趋势数据底座（本期只建表，不展开 UI）
  archive_id TEXT NOT NULL,
  course_code TEXT NOT NULL,
  term_year TEXT,
  term_semester TEXT,
  score REAL,
  credit REAL,
  PRIMARY KEY (archive_id, course_code, term_year, term_semester)
);
```

迁移策略：`meta.schema_version` 起始为 2；每次变更递增并追加迁移脚本（`storage/migrations/vN.rs`），启动时按序执行；Web 版 IndexedDB 数据经 JSON 全量迁移，不做原库直读（避免双端 schema 耦合）。

## 7. 关键接口（Rust tauri commands 草案）

```rust
// 存储
#[tauri::command] fn db_open() -> Result<(), String>;                       // 初始化/迁移
#[tauri::command] fn db_export(path: String) -> Result<(), String>;         // 全量备份
#[tauri::command] fn db_import(path: String, dry_run: bool) -> Result<ImportReport, String>;
#[tauri::command] fn courses_save(archive: String, course: CourseJson) -> Result<(), String>;
#[tauri::command] fn courses_load(archive: String) -> Result<Vec<CourseJson>, String>;
#[tauri::command] fn settings_save(archive: String, key: String, value: serde_json::Value) -> Result<(), String>;
// 文件系统
#[tauri::command] fn dir_pick_and_remember() -> Result<Option<String>, String>;
#[tauri::command] fn scan_directory(dir: String, recursive: bool) -> Result<Vec<CandidateFile>, String>;
#[tauri::command] fn file_sha256(path: String) -> Result<String, String>;
#[tauri::command] fn watch_directory(dir: String, enabled: bool) -> Result<(), String>;
// 解析
#[tauri::command] fn sniff_file(path: String) -> Result<SniffResult, String>;   // 格式/表头类型/置信度
#[tauri::command] fn backup_now(archive: String) -> Result<String, String>;     // 返回备份路径
```

前端封装 `src/infrastructure/desktop/tauri-client.ts`（invoke 包装，非桌面环境走 Web 版实现）。

## 8. 与 Web 版的关系与基线策略

- **方向**：Web 版继续维护（可部署 GitHub Pages）；桌面版以复制时点（v1.0.1）为基线独立演进
- **同步**：Web 版后续的功能性修复，如涉及计算引擎（`domain/`），以 PR 形式人工合入桌面版（`domain/` 改动极少，成本可控）；UI 层不再同步
- **冻结边界**：`src/domain/` 视为共享算法基线，改动必须带单测；`src/infrastructure/`、`src/ui/` 桌面版自由演进

## 9. 测试策略

| 层           | 方式                                                                                                          |
| ------------ | ------------------------------------------------------------------------------------------------------------- |
| 计算引擎单测 | 复制 Web 版 `tests/unit/*`（calculate/grade-point/result-exclusion/dexie-db 等），v1.0.1 基线全绿为 M1 验收门 |
| 组件单测     | 复用 `tests/components/*`；新增迁移向导、导入队列组件测试                                                     |
| 迁移测试     | 用 fixture JSON（模拟 Web 导出）→ 导入 SQLite → 断言数据与计算结果一致                                        |
| Rust 单测    | `cargo test`：schema 迁移链、sha256 去重、嗅探判定、备份校验和                                                |
| E2E          | Playwright（`pnpm test:e2e`，桌面壳内 dev server）；Tauri 层手工验收清单                                      |

## 10. 发布与体积预算

| 项                      | 预算                                                       |
| ----------------------- | ---------------------------------------------------------- |
| Tauri 运行时 + 前端产物 | 5–15 MB                                                    |
| SQLite 动态库（内置）   | < 1 MB                                                     |
| NSIS 安装包 + 卸载器    | +2–5 MB                                                    |
| OCR 模型                | **不进安装包**（首次使用按需下载到 `%LOCALAPPDATA%` 缓存） |
| **合计（预期）**        | **≈ 10–20 MB，远低于 1GB 红线**                            |

要求：`tauri build` 产物在 CI（GitHub Actions：`tauri-apps/tauri-action`）产出；WebView2 用系统版（Win10/11 预装，不捆绑 Bootstrapper 以省体积）。

## 11. 风险清单

| 风险                        | 应对                                                       |
| --------------------------- | ---------------------------------------------------------- |
| 文件名无规律导致误识别      | 内容嗅探为主 + 低置信度人工确认，绝不静默导入（§M3）       |
| PDF 表格提取不稳            | P0 不承诺 PDF；兜底引导同源 xlsx/csv（§M6）                |
| SQLite schema 与 Web 漂移   | schema_version + 迁移脚本链；`domain/` 冻结 + 单测门（§8） |
| 备份损坏                    | manifest 校验和 + 恢复前 dry-run（§M2）                    |
| Tauri 学习成本              | M1 只做壳，前端零改动；Rust 代码集中在 `src-tauri/`        |
| 体积失控（误入大模型/资源） | 发布管线 CI 校验：安装包 > 100MB 报警（§10）               |

## 12. 里程碑验收汇总

| 阶段 | 通过标准                                                  |
| ---- | --------------------------------------------------------- |
| M1   | 桌面壳跑通 Web 版全功能；包 < 30MB；复制基线单测全绿      |
| M2   | 双端数据互通；备份/恢复 + 损坏校验通过                    |
| M3   | 乱命名混合目录正确批量导入；失败隔离；去重生效            |
| M4   | 脏数据清洗 + 多规则集对照正确                             |
| M5   | 目录监听自动导入；定时备份；导出直写                      |
| M6   | PDF 导入可用（或兜底引导）；反推/敏感度可用；OCR 按需下载 |
